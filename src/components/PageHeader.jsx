const PageHeader = ({text}) => {
    return (
        <div className="page-header">
            <h1 className="text-xl text-vbold text-blue">{text}</h1>
        </div>
    )
}

export default PageHeader